{
  "name": "ts-go-run-types-monorepo",
  "version": "0.1.0",
  "description": "Compile-time type resolver for mion runtypes on TypeScript 7 / typescript-go (tsgo).",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.1",
  "homepage": "https://github.com/mionkit/ts-run-types#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mionkit/ts-run-types.git"
  },
  "bugs": {
    "url": "https://github.com/mionkit/ts-run-types/issues"
  },
  "license": "MIT",
  "author": "ma-jerez",
  "engines": {
    "node": ">= 24.0.0",
    "pnpm": ">= 10.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "format": "prettier --write 'packages/**/*.{ts,md}'",
    "check-format": "prettier --check 'packages/**/*.{ts,md}'",
    "check-format-pre-commit": "prettier --check",
    "lint": "lerna run lint",
    "lint-pre-committ": "eslint --quiet",
    "build": "lerna run build",
    "clean": "nx reset && lerna run clean",
    "fresh-start": "lerna run fresh-start && rimraf node_modules && pnpm install --frozen-lockfile",
    "prepare": "husky",
    "pre-publish-test": "bash scripts/pre-publish-test.sh",
    "npm-publish": "bash scripts/publish.sh",
    "npm-unpublish": "bash scripts/unpublish.sh"
  },
  "lint-staged": {
    "packages/**/!(*.spec|*.test|vitest.config|vite.config).ts": [
      "pnpm run lint-pre-committ --",
      "pnpm run check-format-pre-commit --"
    ],
    "packages/**/*.{spec,test}.ts": [
      "pnpm run check-format-pre-commit --"
    ]
  },
  "devDependencies": {
    "@eslint/js": "9.39.4",
    "@types/node": "24.12.2",
    "eslint": "9.39.4",
    "husky": "9.1.7",
    "lerna": "9.0.7",
    "lint-staged": "16.4.0",
    "nx": "22.4.4",
    "prettier": "3.8.2",
    "rimraf": "6.1.3",
    "typescript": "5.5.4",
    "typescript-eslint": "8.58.1",
    "vitest": "2.1.9"
  }
}
