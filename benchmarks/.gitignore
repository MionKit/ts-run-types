# Dependencies & build output (live only inside the container / generated)
node_modules
dist
.vite

# Extra CA certs injected for proxied builds (never commit certs)
.cacerts/*
!.cacerts/.gitkeep

# Logs
*.log
