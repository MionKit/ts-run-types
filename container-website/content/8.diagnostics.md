---
title: Diagnostics
description: Every build-time diagnostic code, what it means, and how to act on it.
navigation:
  icon: i-lucide-triangle-alert
toc: false
---

RunTypes flags type problems while your project builds, each as a short code like `VL010` or `PJ001`. The letters name the feature (`VL` is `createValidate`, `PJ` is `prepareForJson`), and the number marks the position: `001` to `009` are root errors, `010` and up are member-level warnings.

::diagnostic-catalog
::
