---
title: Serialization Suite
description: JSON and binary round-trips — Date, BigInt, Map and Set survive the wire — every case from the serialization suite, with the generated codecs.
toc: false
---

# Serialization Suite

The same `Order`/`User` shape, one more time — and this is where the `Date` earns its keep. JSON drops it on the floor; ts-run-types round-trips it. Binary makes the whole thing compact.

::real-world-scenario
<code-import path="packages/examples/src/suites/realworld.ts" lang="ts" />
::

## What the table shows

Below is every case in the **serialization** test suite, rendered straight from the suite data — JSON encode/decode and the binary codec, across the same type zoo the validators cover. `Date`, `BigInt`, `Map`, `Set` and the `Temporal.*` types survive the trip; decoders hand you back the data-only projection.

Hover (or tap / focus) any row to reveal the **type declaration**, the equivalent `RT.*` **schema** when one exists, and the **generated encoder/decoder code** the build emits.

::suite-table{suite="serialization"}
::
