---
title: Format Validation Suite
description: Type-format validation — email, uuid, ipv4, int32 and friends — baked into your types and checked at the boundary, shown case by case.
toc: false
---

# Format Validation Suite

Same `Order`/`User` shape that anchors every page — note the `FormatUUIDv4` and `FormatEmail` fields. Those aren't bare strings: the format travels with the type, and the validator checks the actual pattern.

::real-world-scenario
<code-import path="packages/examples/src/suites/realworld.ts" lang="ts" />
::

## What the table shows

Below is every case in the **format-validation** test suite, rendered straight from the suite data. Formats are the part where a `string` stops being "any string" — `email`, `uuidv4`, `ipv4`, `int32`, `positive`, the date/time string formats — and starts being *that* string.

Hover (or tap / focus) any row to reveal the **type declaration**, the equivalent `RT.*` **schema** when one exists, and the **generated JavaScript** that enforces the format at runtime.

::suite-table{suite="format-validation"}
::
