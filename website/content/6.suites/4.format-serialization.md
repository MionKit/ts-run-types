---
title: Format Serialization Suite
description: Serializing format-typed fields — how branded strings and constrained numbers round-trip through JSON and binary — case by case.
toc: false
---

::real-world-scenario
<code-import path="packages/examples/src/suites/realworld.ts" lang="ts" />
::

## What the table shows

Below is every case in the **format-serialization** test suite, rendered straight from the suite data — what happens when format-typed fields (`email`, `uuidv4`, the number and date/time formats) go through JSON and binary. The format is metadata; the wire value stays a plain serializable primitive, and the decoded type keeps its brand.

Hover (or tap / focus) any row to reveal the **type declaration**, the equivalent `RT.*` **schema** when one exists, and the **generated codec code**.

::suite-table{suite="format-serialization"}
::
