---
title: Validation Benchmarks
description: How ts-run-types validators stack up against the field — ops/sec across the same real type, with every competitor's source shown inline.
toc: false
---

# Validation Benchmarks

Same `Order`/`User` shape that anchors the test suites — because a benchmark only means something if everyone is checking the *same* thing. Here's the type every contender below validates:

::real-world-scenario
<code-import path="packages/examples/src/suites/realworld.ts" lang="ts" />
::

## What the table shows

The numbers below are generated from the actual benchmark runs — validations per second for ts-run-types and the libraries it's measured against, across a spread of representative types. Higher is better; `—` means no result, `FAIL` means the validator rejected valid input, and `n-a` means the library doesn't support that case.

Hover (or tap / focus) any row to see exactly **how each competitor was set up** — the real source we benchmarked, so you can judge whether the comparison is fair instead of taking our word for it.

::bench-table{bench="validation"}
::
