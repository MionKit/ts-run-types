---
title: Validation
description: How fast is the boolean is-valid check? ops/sec for createValidate across the same real types, with every competitor's source shown inline.
toc: false
pageClass: wide-page
---

## Is-valid — `createValidate`

The cheap boolean "does this match?" check — the common yes/no hot path. Higher is better; `—` = no result, `FAIL` = rejected valid input, `n-a` = unsupported. Hover any row for each competitor's source.

::note
Boolean check only — the heavier error report is a [separate benchmark](/benchmarks/getvalidationerrors). zod has no boolean-only path, so it shows `n-a` here.
::

::bench-table{bench="validation" metric="validate"}
::
