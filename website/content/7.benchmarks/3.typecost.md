---
title: Type-Cost Benchmarks
description: What a type costs at runtime — bundle weight and throughput per type shape — measured, not asserted, with every competitor's source inline.
toc: false
pageClass: wide-page
---

## What the table shows

The numbers below are generated from the actual benchmark runs — throughput per type shape, from the trivial to the deliberately nasty. Higher is better; `—` means no result, `FAIL` means a wrong answer on valid input, and `n-a` means the library can't express that shape at all.

Hover (or tap / focus) any row to see the **exact source** each competitor ran, so the comparison is auditable rather than hand-waved.

::bench-table{bench="typecost"}
::
