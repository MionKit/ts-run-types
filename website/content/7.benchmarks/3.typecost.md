---
title: Type Checking
description: What a type costs the TypeScript compiler — type-instantiation count per shape, measured not asserted, with every competitor's source inline.
toc: false
pageClass: wide-page
---

## What the table shows

TypeScript type-instantiation count per shape — **lower is better** (fewer instantiations = cheaper to type-check). `0` is a real value (a free type); `n-a` means the library can't express that shape. Hover any row for each competitor's source.

::bench-table{bench="typecost"}
::
