{"section":"CIRCULAR","key":"object_full_mion_shape","title":"Self-referential object with optional self-ref and Date prop","description":"mion circularRefs.spec.ts 'Circular object' — full mion fixture (number + string + self-ref + Date). Exercises the same self-recursive dependency call as OBJECT.circular_interface but pins the exact mion shape.","notes":["Self-referential shapes are validated recursively. Atomic rules apply at every level (NaN at `n`, Invalid Date at `d`, etc.)."],"pureType":"interface Circular {\n  n: number;\n  s: string;\n  c?: Circular;\n  d?: Date;\n}\nreturn createValidate<Circular>();","schema":"const cir = RT.circular((self) =>\n  RT.object({\n    n: RT.number(),\n    s: RT.string(),\n    c: RT.optional(self),\n    d: RT.optional(RT.date()),\n  })\n);\nreturn createValidate(cir);","generated":"function YOw_RZp9L3z(v){return (typeof v === 'object' && v !== null && Number.isFinite(v.n) && typeof v.s === 'string' && (v.c === undefined || YOw_RZp9L3z(v.c)) && (v.d === undefined || (v.d instanceof Date && !isNaN(v.d.getTime()))))}"}