{"section":"ARRAYS","key":"array_date","title":"array of dates","description":"`Date[]` — each element encodes to an ISO string on the JSON wire and restores to a Date; binary packs each as a fixed 8-byte epoch.","notes":["Per-element Date transform applies recursively over the array; the empty-array sample confirms no element work happens when there are no items."],"pureType":"createJsonEncoder<Date[]>(undefined, {strategy: 'clone'})","schema":"createJsonEncoder(RT.array(RT.date()))","generated":"function NzL_NLLhtDL(v){return v.map(function(e0){return e0.toISOString()})}"}