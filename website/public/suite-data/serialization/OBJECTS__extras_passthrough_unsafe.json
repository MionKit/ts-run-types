{"section":"OBJECTS","key":"extras_passthrough_unsafe","title":"unsafe path preserves extras (mion semantic — JSON.stringify does not strip)","description":"Canonical baseline for the `prepareForJson + JSON.stringify` path: declared children get transformed, structural extras (both top-level and nested-in-declared-composites) pass through unchanged. Mirrors mion's `03JsonObjects.spec.ts` strip-extras case where the strip expectation is explicitly commented out (`// native JSON.stringify do not strip extra params`). The safe path (`stripUnknownKeys + prepareForJson + JSON.stringify`) strips the extras — that divergence is exercised in EXTRA_PARAMS.","notes":["Strategy split: `mutate` walks declared children only and lets `JSON.stringify` pass undeclared extras through, so `getTestData` round-trips them unchanged; `clone` and `direct` are shape-derived and strip extras pre-serialise, so `getTestDataForStringify` restores the declared-only shape (`deserializedValues` drops the extras).","Decode split: the `preserve` decoder passes undeclared keys through to the restored value, while the default `strip` decoder nukes them to `undefined`."],"pureType":"createJsonEncoder<{\n        startDate: Date;\n        quantity: number;\n        name: string;\n        nullValue: null;\n        stringArray: string[];\n        bigInt: bigint;\n        optionalString?: string;\n        \"weird prop name \\n?>'\\\\\\t\\r\": string;\n        deep: {a: string; b: number};\n        '?other weird p': {c: string; d: number};\n      }>(undefined, {strategy: 'clone'})","schema":"JsonEncoder(\n  RT.object({\n    startDate: RT.date(),\n    quantity: RT.number(),\n    name: RT.string(),\n    nullValue: RT.literal(null),\n    stringArray: RT.array(RT.string()),\n    bigInt: RT.bigint(),\n    optionalString: RT.optional(RT.string()),\n    \"weird prop name \\n?>'\\\\\\t\\r\": RT.string(),\n    deep: RT.object({a: RT.string(), b: RT.number()}),\n    '?other weird p': RT.object({c: RT.string(), d: RT.number()}),\n  })\n)","generated":"import {__rt_NzL_ie4yiaU} from 'virtual:rt/NzL_ie4yiaU.js';\nexport const __rt_BPD_ie4yiaU=['jeCL',()=>[__rt_NzL_ie4yiaU],,'BPD_ie4yiaU','objectLiteral','const pjsFn = utl.getRT(\\'NzL_ie4yiaU\\').fn;function BPD_ie4yiaU(v){return JSON.stringify(pjsFn(v));}return BPD_ie4yiaU'];\n\n\nexport const __rt_NzL_ie4yiaU=['pjs',,,'NzL_ie4yiaU','objectLiteral','const ctxFn0 = function(v){if (Object.keys(v.deep).length === 2) return v.deep;return {a:v.deep.a,b:v.deep.b}};\\nconst ctxFn1 = function(v){if (Object.keys(v[\\'?other weird p\\']).length === 2) return v[\\'?other weird p\\'];return {c:v[\\'?other weird p\\'].c,d:v[\\'?other weird p\\'].d}};\\nconst ctxFn2 = function(v){const _r={startDate:v.startDate.toISOString(),quantity:v.quantity,name:v.name,nullValue:v.nullValue,stringArray:v.stringArray,bigInt:v.bigInt.toString(),\\'weird prop name \\\\n?>\\\\\\'\\\\\\\\\\\\t\\\\r\\':v[\\'weird prop name \\\\n?>\\\\\\'\\\\\\\\\\\\t\\\\r\\'],deep:ctxFn0(v),\\'?other weird p\\':ctxFn1(v)};if (v.optionalString !== undefined) _r[\\'optionalString\\']=v.optionalString;return _r;};function NzL_ie4yiaU(v){return ctxFn2(v)}return NzL_ie4yiaU'];"}