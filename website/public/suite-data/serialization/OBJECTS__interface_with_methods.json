{"section":"OBJECTS","key":"interface_with_methods","title":"methods should be excluded from interface when serializing","description":"Interface with a string field plus a function-typed `methodProp`. The non-serializable method is dropped from the projection, so the restored shape keeps only the data field (asymmetric deserializedValues).","notes":["The function-typed property is non-serializable and silently dropped on the wire; only the string field round-trips."],"pureType":"interface ObjectWithMethods {\n  name: string;\n  methodProp: () => any;\n}\nreturn createJsonEncoder<ObjectWithMethods>(undefined, {strategy: 'clone'});","schema":"createJsonEncoder(RT.object({name: RT.string(), methodProp: RT.func([], RT.any())}))","generated":"function NzL_l7HFZPH(v){if (Object.keys(v).length === 1) return v;return {name:v.name}}"}